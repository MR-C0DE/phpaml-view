# Contributing

1. Create a focused branch from `main`.
2. Keep the public API PHP-first, frontend-focused and accessible.
3. Add tests for every behavior or bug fix.
4. Run `composer test` and `composer validate --strict`.
5. Document public API changes in the README, API reference and changelog.

Do not commit secrets, generated dependency folders or local runtime data.
